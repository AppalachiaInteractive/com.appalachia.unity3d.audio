namespace Appalachia.Core.AssetMetadata.AudioMetadata.Context
{
    public enum GroundCondition_AudioContexts : short
    {
        Normal = 0,
        Wet = 10,
        Snow = 20,
        Frozen = 30,
    }
}
